# CV-lab-1

# Tasks:

- Person 1:
  - Create color painting using bilteral filter (don't miss optimization).
  - Combine sketch with the color painting.
- Person 2:
  - Create black and white sketch (convert img to gray scale - apply median filter - apply laplacian filter - apply binary threshold).
  - Apply 2D median filter to lane road.
  - Apply Canny edge detector.
  - Define ROI mask to the road.
- Person 3:
  - Implement hough transform (voting - accumaltor array).
  - Plot accumalotr array.
  - Non maximum supression & Threshold.
  - Plot the lines back to the image